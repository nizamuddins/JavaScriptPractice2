// conversion of kilometers into miles

const kilometers = +prompt("Enter the Kilometer value ");

const factor = 0.62137;

const miles = kilometers*factor;

console.log(`${kilometers} is equals to ${miles} miles`);
