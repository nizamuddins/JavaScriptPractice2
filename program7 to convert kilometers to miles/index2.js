// conversion of Miles into kilometers

const Miles = +prompt("Enter the Miles value ");

const factor = 0.62137;

const kilometers = Miles/factor;

console.log(`${Miles} is equals to ${kilometers} kilometers`);
